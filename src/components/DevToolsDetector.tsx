"use client";

/**
 * DevTools detection has been disabled as per user request.
 * Previously blocked right-click, keyboard shortcuts, and text selection.
 */
export function DevToolsDetector() {
    return null;
}
